// Catergories of Operators 

// Assigment =
// Arithmetic +;

for ( i = 0; i <= 100; i++) {
     let x = i % 5;
    if ( x == 0 ) {
        console.log('no remainder');
    }
    else {
        console.log('the remainder is ', x )
    }
}